# SSPanel 对接 ssr 多端口

> 该教程是 ssr 多端口多用户教程，ssr 单端口多用户请参看：[跳转](ssr/sspanel-ssr-single-port.md)

# 第一步，在面板添加一个 Shadowsocks 节点

非常简单，教程省略

# 第二步，配置 heki

参考: [heki 详细配置项](heki/heki-config.md)

# 第三步，启动 heki

```
heki start
```

若出现启动失败的情况，使用`heki log`查看错误信息
